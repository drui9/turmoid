env := ./venv
rundir := ./droid
pip := $(env)/bin/pip
python := $(env)/bin/python
build_deps := $(rundir)/requirements.txt


run: $(clean) $(env)
	$(python) $(rundir)/droid.py

$(env):
	python3 -m venv $@ && $(pip) install -r $(build_deps)

clean:
	@rm -rf **/__pycache__
